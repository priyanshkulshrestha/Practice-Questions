// let, var, const (scope, hoisting,    , reinitialization, tdz (temporal dead zone))
// defer and async to script tag (critical rendering path)
// setTimeout, setInterval, clearInterval, clearTimeout (event loop)
// functional programming, arrow functions
// async await
// promises
// promises chaining
// callback &  callback hell
// higher order functions
// this keyword, call, apply, bind
// closure
// compose, currying, memoization
// event bubbling, event capturing, event delegation
// event loop
// polyfill
//currying
// prototypial inheritance 
// debouncing, throttling
// Generators
// function declaration, function expression
// IIFE

// Indexing in db

// The || operator (logical OR) returns the first truthy operand, or the last operand if none are truthy.
// The && operator (logical AND) returns the first falsy operand, or the last operand if all are truthy.

// console.log( 0 || 1) // -- 1
// console.log( 2 || 1) // -- 2
// console.log( 2 && 1) // -- 1
// console.log( 1 && 2) // -- 2